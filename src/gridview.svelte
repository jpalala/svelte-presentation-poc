function renderGrid(description, i) {
  = item.description;
 const boxtemplate = ``;
 document.getElementById("slidersGrid").innerHTML += `<div class="flexible centered">${description}</div>`;
}
// get all the slides' descriptions fields
slideDescriptions = var new_array = arr.map(function callback(element, index, array) {
    // Return value for new_array
}[, thisArg])

slideDescriptions.forEach(renderGrid);

<>